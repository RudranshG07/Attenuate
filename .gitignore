# secrets
.env
.env.*
!.env.example
*.key
keystore/

# node
node_modules/
dist/
.next/
*.tsbuildinfo

# foundry
contracts/out/
contracts/cache/
contracts/broadcast/
# leftover forge install at repo root (real deps are git submodules in contracts/lib/)
/lib/

# subgraph
subgraph/build/
subgraph/generated/

# indexer state: a cursor is where the last run got to, not a fact about the project
indexer/.cursor
deployments/indexer-cursor.json

# docs
*.md
!/README.md
# ETHOnline requires spec-driven submissions to include their spec and planning
# artifacts, so the implementation spec ships. The strategy doc deliberately does not.
!/SPEC.md
!/mcp-server/SKILL.md
*.bak
*.docx

# os
.DS_Store

# next
web/.next/
next-env.d.ts
!/AI-USE.md
!/PARTNERS.md
!/FEEDBACK.md
!/prompts/
!/prompts/*.md

# generated by scripts/plan.ts; a run record, not a fixture
deployments/planner-log.jsonl
